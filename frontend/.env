# This is only needed if you want to change the backend URL (highly recommended)
REACT_APP_API_URL=http://localhost:5000/api

# Optional: skip pre-flight warnings in dev (not required)
GENERATE_SOURCEMAP=false
